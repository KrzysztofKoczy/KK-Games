# Skopiuj ten plik jako .env i uzupełnij dane

# Port serwera
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=games_app
DB_USER=postgres
DB_PASSWORD=twoje_haslo_tutaj

# JWT Secret (dla przyszłości - logowanie)
JWT_SECRET=twoj-secret-key-tutaj-wygeneruj-losowy-klucz

# Socket.io CORS
CORS_ORIGIN=http://localhost:8100

